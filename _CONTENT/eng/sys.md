---
---
```
uniq ids
    uuid: not-sortable
    id-server: spof
    snowflake id: timestamp + data center id + machine id + sequence number


rate limiter 
    token bucket 
    leaky bucket 
    sliding window with weight 

    redis incr 
    k8s ingress controller 

file sys 
    metadata vs data
    inode blocks levels
    tree


maps 
    point vs vector
    static vs dynamic

    s2
    geohash
    tiles

docs 
    rds + s3 + cdn 
    websocket + OT
    edit q 
    version deltas, snapshots

topk
    redis sorted set, sharded in cluster
    zset zrevrange
    batch updates 

notif. 
    q workers templates 
    SSE webhooks 


streaming
payments
search engine
chat
maps 
news
trader
```